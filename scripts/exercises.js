const request = new Request("../exercises/exercises.json")


// const requestObject = new XMLHttpRequest()

// // Handle request
// requestObject.onreadystatechange = () => {
//     (this.readyState == 4 && this.status == 200)
// }

// // Nothin in the application works without a current exercise, so fetch syncronously
// requestObject.open("GET", "../exercises/exercises.json", false);